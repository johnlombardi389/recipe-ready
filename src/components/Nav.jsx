import { NavLink } from "react-router-dom";
// Style
import styled from "styled-components";

const Nav = () => {
  return (
    <nav>
      <h1>This is the nav</h1>
    </nav>
  );
};

export default Nav;
